<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gk.jobhelper.mapper.JobPositionMapper">

    <sql id="Base_Column_List">
        id, exam_id, import_file_id, department_name, organization_name, position_name, position_code,
        province, city, district, recruit_count, education_requirement, degree_requirement,
        major_requirement, major_codes, age_requirement, political_requirement, work_year_requirement,
        fresh_graduate_requirement, household_requirement, service_project_requirement,
        certificate_requirement, gender_requirement, position_description, remark,
        source_sheet, source_row, raw_data, created_at, updated_at
    </sql>

    <!-- 列表查询列：不含 raw_data 大字段 -->
    <sql id="List_Column_List">
        id, exam_id, import_file_id, department_name, organization_name, position_name, position_code,
        province, city, district, recruit_count, education_requirement, degree_requirement,
        major_requirement, major_codes, age_requirement, political_requirement, work_year_requirement,
        fresh_graduate_requirement, household_requirement, service_project_requirement,
        certificate_requirement, gender_requirement, position_description, remark,
        source_sheet, source_row, created_at, updated_at
    </sql>

    <resultMap id="BaseResultMap" type="com.gk.jobhelper.entity.JobPosition">
        <id property="id" column="id"/>
        <result property="examId" column="exam_id"/>
        <result property="importFileId" column="import_file_id"/>
        <result property="departmentName" column="department_name"/>
        <result property="organizationName" column="organization_name"/>
        <result property="positionName" column="position_name"/>
        <result property="positionCode" column="position_code"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="recruitCount" column="recruit_count"/>
        <result property="educationRequirement" column="education_requirement"/>
        <result property="degreeRequirement" column="degree_requirement"/>
        <result property="majorRequirement" column="major_requirement"/>
        <result property="majorCodes" column="major_codes"/>
        <result property="ageRequirement" column="age_requirement"/>
        <result property="politicalRequirement" column="political_requirement"/>
        <result property="workYearRequirement" column="work_year_requirement"/>
        <result property="freshGraduateRequirement" column="fresh_graduate_requirement"/>
        <result property="householdRequirement" column="household_requirement"/>
        <result property="serviceProjectRequirement" column="service_project_requirement"/>
        <result property="certificateRequirement" column="certificate_requirement"/>
        <result property="genderRequirement" column="gender_requirement"/>
        <result property="positionDescription" column="position_description"/>
        <result property="remark" column="remark"/>
        <result property="sourceSheet" column="source_sheet"/>
        <result property="sourceRow" column="source_row"/>
        <result property="rawData" column="raw_data"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM job_position
        WHERE id = #{id}
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/> FROM job_position WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <select id="selectByPositionCodes" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/> FROM job_position WHERE position_code IN
        <foreach collection="codes" item="code" open="(" separator="," close=")">#{code}</foreach>
        ORDER BY id DESC
    </select>

    <select id="selectByImportFileIdAndPositionCodes" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/> FROM job_position
        WHERE import_file_id = #{importFileId} AND position_code IN
        <foreach collection="codes" item="code" open="(" separator="," close=")">#{code}</foreach>
        ORDER BY id DESC
    </select>

    <select id="selectByImportFileId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM job_position
        WHERE import_file_id = #{importFileId}
        ORDER BY source_row ASC
    </select>

    <select id="countByImportFileId" resultType="long">
        SELECT COUNT(*)
        FROM job_position
        WHERE import_file_id = #{importFileId}
    </select>

    <select id="selectPageByImportFileId" resultMap="BaseResultMap">
        SELECT
        <include refid="List_Column_List"/>
        FROM job_position
        WHERE import_file_id = #{importFileId}
        ORDER BY source_row ASC
            LIMIT #{offset}, #{size}
    </select>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO job_position (
            exam_id, import_file_id, department_name, organization_name, position_name, position_code,
            province, city, district, recruit_count, education_requirement, degree_requirement,
            major_requirement, major_codes, age_requirement, political_requirement, work_year_requirement,
            fresh_graduate_requirement, household_requirement, service_project_requirement,
            certificate_requirement, gender_requirement, position_description, remark,
            source_sheet, source_row, raw_data, created_at, updated_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.examId}, #{item.importFileId}, #{item.departmentName}, #{item.organizationName},
                #{item.positionName}, #{item.positionCode}, #{item.province}, #{item.city}, #{item.district},
                #{item.recruitCount}, #{item.educationRequirement}, #{item.degreeRequirement},
                #{item.majorRequirement}, #{item.majorCodes}, #{item.ageRequirement},
                #{item.politicalRequirement}, #{item.workYearRequirement}, #{item.freshGraduateRequirement},
                #{item.householdRequirement}, #{item.serviceProjectRequirement}, #{item.certificateRequirement},
                #{item.genderRequirement}, #{item.positionDescription}, #{item.remark},
                #{item.sourceSheet}, #{item.sourceRow}, #{item.rawData}, #{item.createdAt}, #{item.updatedAt}
            )
        </foreach>
    </insert>

    <sql id="Query_Condition">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (position_name LIKE #{keyword}
                OR position_code LIKE #{keyword}
                OR department_name LIKE #{keyword}
                OR organization_name LIKE #{keyword})
            </if>
            <if test="departmentName != null and departmentName != ''">
                AND department_name LIKE #{departmentName}
            </if>
            <if test="organizationName != null and organizationName != ''">
                AND organization_name LIKE #{organizationName}
            </if>
            <if test="educationRequirement != null and educationRequirement != ''">
                AND education_requirement LIKE #{educationRequirement}
            </if>
            <if test="majorRequirement != null and majorRequirement != ''">
                AND major_requirement LIKE #{majorRequirement}
            </if>
            <if test="province != null and province != ''">
                AND province LIKE #{province}
            </if>
            <if test="city != null and city != ''">
                AND city LIKE #{city}
            </if>
        </where>
    </sql>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="List_Column_List"/>
        FROM job_position
        <include refid="Query_Condition"/>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*)
        FROM job_position
        <include refid="Query_Condition"/>
    </select>

    <delete id="deleteByImportFileId">
        DELETE FROM job_position
        WHERE import_file_id = #{importFileId}
    </delete>

    <delete id="deleteAll">
        DELETE FROM job_position
    </delete>
</mapper>
