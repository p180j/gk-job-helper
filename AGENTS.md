# 项目协作约定

## 多处开发与环境兼容

用户会在多台电脑、多个目录中开发本项目。修改代码、配置、启动脚本、数据库结构或依赖前，必须判断改动是否依赖当前电脑的路径、系统环境、端口、已安装软件或本地数据。

当用户询问更换电脑、开发地点或项目路径，或者当前改动可能影响其他开发环境时，必须主动说明：

- 改动属于项目共享代码，还是仅限当前电脑的本地配置。
- 共享代码改动会影响哪些功能、接口、页面、启动流程或旧版本行为，是否会与其他电脑上的未同步代码产生冲突。
- 是否会影响其他电脑上已有的启动方式、配置或数据。
- 其他电脑在拉取或运行新代码前需要完成哪些操作，例如备份、修改路径、配置环境变量、更新数据库或重新安装依赖。

每次完成可能跨环境传播的代码改动后，最终回复中必须单独说明“代码影响”和“其他开发环境需要处理”。即使不需要额外操作，也要明确写明“不需要额外处理”，不能只说明当前电脑已经验证通过。

应优先使用相对路径、项目目录自动识别和可覆盖的环境配置，避免把单台电脑的绝对路径直接写入共享代码。无法避免环境差异时，必须明确提醒用户，不能静默修改后直接视为所有环境都兼容。

## 整体设计与可维护性

每次新增或修改逻辑时，不得只针对当前报错、单个样例或单台电脑以“打补丁”的方式不断叠加特判。开始修改前必须先理解相关模块的职责、完整数据链路、已有抽象和上下游影响，在保证功能完整性的前提下，从整个项目的一致性与长期可维护性出发设计实现。

具体要求：

- 优先形成统一、清晰、可复用的实现，不在 Controller、Service、前端页面或启动脚本中分散堆放同类判断。
- 如果问题暴露出现有职责划分或抽象不合理，应评估并整理正确的模块边界，而不是通过新增分支掩盖结构问题。
- 新增兼容或兜底逻辑必须属于明确的整体兼容策略，说明适用边界、主流程与失败行为，并配套真实场景测试；不得用兜底吞掉错误或隐藏数据质量问题。
- 修改时必须同时检查数据库模型、后端服务、接口 DTO、前端类型与展示、测试和启动流程的一致性，避免只打通局部链路。
- 保持命名、错误处理、日志、配置方式和代码风格在项目内一致；发现重复实现时应优先复用或抽取公共能力。
- 完成后不仅验证当前样例，还要验证原有功能、边界情况和其他开发环境，明确说明整体影响以及仍存在的限制。
